{% extends 'base.html.twig' %}

{% block title %}{{ user.lastname }} {{ user.irstname }}{% endblock %}

{% block body %}
    <h1>{{ user.lastname }} {{ user.irstname }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ user.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ user.lastname }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-light"  href="{{ path('student_index') }}">Retour à la liste</a>

    <a class="btn btn-success"  href="{{ path('student_edit', {'id': user.id}) }}"><i class="fas fa-pen p-r-5"></i>Editer</a>

    {{ include('students/_delete_form.html.twig') }}
{% endblock %}
